events {}

http {
    upstream backend {
        server mini-backend-1:8000;
        server mini-backend-2:8000;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://backend;
        }
    }
}
